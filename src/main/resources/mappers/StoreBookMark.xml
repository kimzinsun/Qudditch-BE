<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goldensnitch.qudditch.mapper.CustomerBookmarkStore">
    <!--북마크 설정-->
    <insert id="storeBookmark" parameterType="CustomerBookmarkStore">
        insert into customer_bookmark_store (user_customer_id, user_store_id)
        values ($(userCustomerId), $(userStoreId))
    </insert>


</mapper>