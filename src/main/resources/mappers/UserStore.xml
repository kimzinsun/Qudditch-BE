<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goldensnitch.qudditch.repository.UserStoreRepository">
    
    <!-- 새로운 가게유저 추가 -->
    <insert id="insertUserStore" parameterType="com.goldensnitch.qudditch.dto.UserStore">
        INSERT INTO user_store (email, password, name, bn_number, state) VALUES (#{email}, #{password}, #{name}, #{bnNumber}, #{state})
    </insert>

    <!-- 이메일로 점주 검색 -->
    <select id="findByEmail" parameterType="String" resultType="com.goldensnitch.qudditch.dto.UserStore">
        SELECT * FROM user_store WHERE email = #{email}
    </select>
    <!-- 가게유저 검색 -->
    <select id="selectUserStoreById" parameterType="int" resultType="com.goldensnitch.qudditch.dto.UserStore">
        SELECT * FROM user_store WHERE id = #{id}
    </select>

    <!-- 가게유저 수정 -->
    <!-- <update id="updateUserStore" parameterType="com.goldensnitch.qudditch.dto.UserStore">
        UPDATE user_store SET
            store_id = #{storeId},
            email = #{email},
            password = #{password},
            name = #{name},
            bn_number = #{bnNumber},
            state = #{state},
            created_at = #{createdAt},
            modified_at = #{modifiedAt}
        WHERE id = #{id}
    </update> -->

    <!-- 가제유저 삭제 -->
    <delete id="deleteUserStore" parameterType="int">
        DELETE FROM user_store WHERE id = #{id}
    </delete>
    
    <!-- 추가 쿼리는 사용 사례에 맞게 작성 -->

</mapper>
