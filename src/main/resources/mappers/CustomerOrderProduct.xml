<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goldensnitch.qudditch.mapper.CartMapper">

    <!-- 주문 항목 추가 -->
    <insert id="addItemToCustomerOrder" parameterType="com.goldensnitch.qudditch.dto.CustomerOrderProduct">
        INSERT INTO customer_order_products (customer_order_id, product_id, qty)
        VALUES (#{customerOrderId}, #{productId}, #{qty})
    </insert>

    <!-- 주문 상세 정보 조회 -->
    <select id="getCustomerOrderDetails" resultType="com.goldensnitch.qudditch.dto.CustomerOrderProduct">
        SELECT product_id, qty
        FROM customer_order_products
        WHERE customer_order_id = #{customerOrderId}
    </select>

</mapper>